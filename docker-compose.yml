version: '3.8'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - RATE_LIMITER_MODE=redis
      - REDIS_HOST=redis
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
